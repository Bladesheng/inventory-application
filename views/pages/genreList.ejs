<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head", {title}) %>

  <body>
    <ul>
      <% genres.forEach((genre) => { %>
      <li>
        <a href="<%= genre.url %> "><%= genre.name %></a>
      </li>
      <% }) %>
    </ul>

    <%- include("../partials/footer") %>
  </body>
</html>
