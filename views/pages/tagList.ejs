<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head", {title}) %>

  <body>
    <%- include("../partials/navbar") %>

    <ul>
      <% tags.forEach((tag) => { %>
      <li>
        <a href="<%= tag.url %> "><%= tag.name %></a>
      </li>
      <% }) %>
    </ul>

    <%- include("../partials/footer") %>
  </body>
</html>
