<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head", {title}) %> <%- include("../partials/navbar") %>

  <body>
    <ul>
      <% tags.forEach((tag) => { %>
      <li>
        <a href="<%= tag.url %> "><%= tag.name %></a>
      </li>
      <% }) %>
    </ul>

    <%- include("../partials/footer") %>
  </body>
</html>
