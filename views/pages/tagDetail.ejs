<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head", {title}) %> <%- include("../partials/navbar") %>

  <body>
    <h2>Games with <%= tag.name %> tag</h2>
    <ul>
      <% games.forEach((game) => { %>
      <li>
        <a href="<%= game.url %> "><%= game.name %></a>
      </li>
      <% }) %>
    </ul>

    <%- include("../partials/footer") %>
  </body>
</html>
